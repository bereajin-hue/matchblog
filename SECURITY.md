# 매치블로그 (MatchBlog) — 보안 마스터 지침 (SECURITY.md)

> 이 파일은 `CLAUDE.md`의 보안 확장 모듈이다.
> 클로드코드는 모든 기능 구현 시 이 문서의 규칙을 **CLAUDE.md와 동등한 강제 규칙**으로 적용한다.
> 보안 규칙은 기능 구현보다 우선한다. 충돌 시 보안 규칙을 따른다.

---

## 0. 위협 모델 (먼저 무엇을 지키는가)

이 플랫폼이 지켜야 할 자산을 위험도 순으로 정의한다.

| 등급 | 자산 | 유출 시 영향 |
|---|---|---|
| 🔴 치명 | **고객/운영 블로그 계정 자격증명** (네이버·티스토리·Blogger ID/PW, 토큰) | 계정 탈취, 사업 신뢰 붕괴, 법적 책임 |
| 🔴 치명 | 결제정보 (토스 paymentKey, 결제 토큰) | 금전 사고, 분쟁 |
| 🟠 높음 | 개인정보 (이름, 연락처, **사업자번호**) | 개인정보보호법 위반, 과징금 |
| 🟠 높음 | 관리자 계정 / 어드민 접근 권한 | 전체 데이터 탈취, 무단 발행 |
| 🟡 중간 | 업로드 자산 (매장 사진/영상) | 저작권/프라이버시 |
| 🟡 중간 | 무료분석 결과 / 워커 시크릿 | 어뷰징, 무단 호출 |

> **1순위 원칙**: 블로그 계정 자격증명은 평문으로 어디에도 저장하지 않는다.
> DB, 로그, 환경변수 평문, 깃 저장소 어디에도 남기지 않는다.

---

## 1. 보안 절대 규칙 (위반 시 즉시 수정 대상)

1. **시크릿 하드코딩 절대 금지** — API 키, DB 비밀번호, 토스 시크릿키,
   워커 시크릿, 암호화 키를 코드/깃에 직접 쓰지 않는다.
   모두 환경변수 + 시크릿 매니저(Vercel Env, 서버 시크릿)로 관리.
   `.env`는 `.gitignore`에 포함, `.env.example`만 커밋.

2. **자격증명 암호화 저장** — 블로그 계정 정보는 반드시 애플리케이션
   레벨 암호화(AES-256-GCM) 후 저장. 암호화 키는 DB와 분리된 곳에 보관.
   복호화는 워커의 발행 실행 순간에만, 메모리 내에서만 수행.

3. **최소 권한 원칙(PoLP)** — 모든 컴포넌트는 필요한 최소 권한만 가진다.
   - web은 워커의 generate/publish만 호출 가능, 직접 DB 관리권한 금지
   - Supabase는 RLS로 본인 데이터만, service_role 키는 서버에서만 사용
   - 클라이언트(브라우저)에는 anon 키만 노출, service_role 키 절대 노출 금지

4. **모든 입력은 신뢰하지 않는다** — 모든 API 입력은 zod로 검증.
   결제 금액·상품가격·권한은 항상 서버에서 재계산/재확인.

5. **민감정보 로깅 금지** — 비밀번호, 토큰, 자격증명, 사업자번호 전체,
   결제키는 로그·에러메시지·콘솔에 남기지 않는다. 마스킹 처리.

6. **HTTPS 강제 + 보안 헤더** — 모든 통신 TLS. 보안 헤더(아래 4번) 적용.

7. **관리자 경로 강화** — admin은 RBAC + (권장) 2단계 인증/IP 제한.
   모든 관리자 행동은 admin_logs에 감사 기록.

---

## 2. 계층별 보안 하네스 구조

```
[브라우저]
  │  anon 키만, HttpOnly 세션쿠키, CSP 적용
  ▼
[Next.js / Vercel]  ── 인증·인가·입력검증·rate limit·결제검증
  │  service_role 키는 서버 함수에서만
  │  X-Worker-Secret 헤더로 워커 호출
  ▼
[Supabase]  ── RLS 전체 적용, 암호화 컬럼, 감사로그
  ▲
  │  복호화 키는 워커 환경에만
[Worker / Python]  ── 자격증명 복호화는 발행 순간 메모리에서만
                      외부망 화이트리스트, 시크릿 검증
```

### 신뢰 경계 (Trust Boundary)
- **브라우저 ↔ Web**: 외부 입력 전부 불신. 인증·검증·rate limit.
- **Web ↔ Supabase**: service_role은 서버 전용. RLS가 최후 방어선.
- **Web ↔ Worker**: 상호 시크릿 인증. 워커는 web 외 호출 거부.
- **Worker ↔ 외부 블로그**: 자격증명 사용 구간. 가장 강하게 격리.

---

## 3. 자산별 보안 처리 규칙

### 3.1 블로그 계정 자격증명 (최우선)
- 저장: AES-256-GCM 암호화 후 DB 별도 테이블(`blog_credentials`).
- 키 관리: 암호화 마스터키는 워커 서버 환경변수/시크릿 매니저에만.
  DB가 통째로 유출돼도 키 없이는 복호화 불가하도록 분리.
- 사용: 워커가 발행 실행 시에만 복호화, 작업 후 메모리에서 즉시 폐기.
- 절대 금지: 평문 저장, 로그 출력, web/브라우저로 전달, 깃 커밋.

### 3.2 개인정보 (이름/연락처/사업자번호)
- RLS로 본인 + 관리자만 접근.
- 사업자번호는 표시 시 마스킹(예: 123-45-***89).
- URL 쿼리스트링에 개인정보 절대 미포함.
- 개인정보처리방침 게시 + 수집·이용 동의 기록.

### 3.3 결제정보
- 카드정보는 직접 다루지 않는다(토스가 처리). paymentKey만 보관.
- 결제 승인은 서버에서 amount 재검증 후 confirm.
- 웹훅은 토스 서명 검증으로 위조 차단.

### 3.4 업로드 자산 (사진/영상)
- Supabase Storage 비공개 버킷 + 서명 URL(만료시간 설정).
- 업로드 시 MIME/확장자/용량 검증, 이미지·영상만 허용.
- 파일명 난수화(원본명 노출 금지), 실행파일 업로드 차단.

---

## 4. 웹 애플리케이션 보안 (Next.js)

- **인증**: Supabase Auth 세션은 HttpOnly + Secure + SameSite 쿠키.
  토큰을 localStorage에 저장하지 않는다(XSS 탈취 방지).
- **인가**: 미들웨어로 (user)/admin 라우트 보호.
  서버 액션/Route Handler마다 권한 재확인(클라 신뢰 금지).
- **입력검증**: 모든 입력 zod 스키마. SQL은 파라미터 바인딩(쿼리 조합 금지).
- **보안 헤더** (next.config / middleware):
  - Content-Security-Policy (인라인 스크립트 최소화)
  - Strict-Transport-Security (HSTS)
  - X-Frame-Options: DENY (클릭재킹 방지)
  - X-Content-Type-Options: nosniff
  - Referrer-Policy: strict-origin-when-cross-origin
- **CSRF**: 상태변경 요청에 CSRF 토큰 또는 SameSite 쿠키 + Origin 검증.
- **Rate Limiting**: 로그인, 무료분석, 결제, 신청 API에 IP/계정 단위 제한.
- **에러 처리**: 사용자에게는 일반화된 메시지, 상세 스택은 서버 로그에만.

---

## 5. 인증·계정 보안

- 카카오 OAuth state 파라미터로 CSRF 방지, redirect URI 화이트리스트.
- 이메일 로그인: 비밀번호는 Supabase Auth 해시 저장(직접 해싱 금지).
  비밀번호 정책(최소 길이/복잡도), 로그인 실패 제한(브루트포스 방지).
- 세션: 적절한 만료시간, 로그아웃 시 세션 무효화.
- 관리자: 일반 사용자와 권한 격리. 가능하면 별도 도메인/2FA/IP 제한.

---

## 6. 워커(자동화 서버) 보안 — 가장 민감한 구간

- web → worker 호출은 `X-Worker-Secret` 헤더 검증, 없으면 401.
  (권장) 추가로 HMAC 서명 + 타임스탬프로 재전송 공격 방지.
- 워커는 인터넷에 직접 노출하지 않는다(방화벽/프라이빗 네트워크,
  또는 web 서버 IP만 허용).
- 워커의 외부 통신은 발행 대상 도메인(네이버/티스토리/Blogger)만 화이트리스트.
- 자격증명 복호화 키는 워커 환경에만 존재. web과 공유하지 않는다.
- 워커 작업 로그에 자격증명/토큰을 남기지 않는다.

---

## 7. 데이터 보안 (Supabase / RLS)

- **모든 테이블 RLS 활성화.** 기본 정책은 거부(deny), 명시적 허용만.
  - 일반 사용자: 본인 row만 SELECT/필요한 작업.
  - admin role: 운영 데이터 접근(감사로그 기록 전제).
- service_role 키는 서버 환경에서만. 클라이언트 노출 시 RLS 우회됨 → 절대 금지.
- 민감 컬럼(자격증명)은 RLS만으로 부족 → 애플리케이션 암호화 병행.
- 백업 데이터도 암호화. 백업 접근 권한 통제.
- 마이그레이션/스키마 변경은 감사 가능하도록 관리.

---

## 8. 시크릿 / 키 관리

| 시크릿 | 저장 위치 | 노출 범위 |
|---|---|---|
| Supabase anon key | Vercel Env (public) | 브라우저 허용 |
| Supabase service_role | Vercel Env (server) | 서버 전용 |
| 토스 시크릿키 | Vercel Env (server) | 서버 전용 |
| X-Worker-Secret | web + worker 양쪽 server env | 서버 전용 |
| 자격증명 암호화 마스터키 | **워커 server env / 시크릿 매니저** | 워커 전용 |
| 카카오 client secret | Vercel Env (server) | 서버 전용 |

- 키 회전(rotation) 절차를 문서화. 유출 의심 시 즉시 회전.
- 깃 히스토리에 시크릿이 들어갔다면 회전 + 히스토리 정리.

---

## 9. 보안 단계별 개발 프롬프트 (각 STEP에 보안 게이트 삽입)

> 기존 STEP 1~7 진행 시 아래 보안 프롬프트를 해당 STEP에 함께 적용한다.

### SEC-STEP 1 — 기반 보안 셋업 (STEP 1과 함께)
```
보안 기반을 함께 구축한다.
1. .gitignore에 .env, .env.local, 키 파일 포함. .env.example만 커밋.
2. next.config / middleware에 보안 헤더 적용:
   CSP, HSTS, X-Frame-Options:DENY, X-Content-Type-Options:nosniff,
   Referrer-Policy.
3. Supabase service_role 키는 서버 전용 모듈에서만 import 되도록 분리.
   클라이언트 번들에 service_role이 포함되지 않음을 확인.
4. 모든 테이블 RLS 활성화 + 기본 거부 정책. 본인/admin 정책만 명시 허용.
완료 후 보안 헤더와 RLS 정책, 키 분리 구조를 보여주고 검토받아라.
```

### SEC-STEP 2 — 인증 보안 (STEP 2와 함께)
```
인증 보안을 강화한다.
1. 세션은 HttpOnly + Secure + SameSite 쿠키. 토큰 localStorage 저장 금지.
2. 카카오 OAuth에 state 파라미터(CSRF 방지), redirect URI 화이트리스트.
3. 이메일 로그인 실패 횟수 제한(브루트포스 방지) + 비밀번호 정책.
4. admin 라우트는 미들웨어 + 서버단 role 재확인 이중 보호.
5. 로그인/회원 API에 rate limiting.
민감정보가 로그에 남지 않는지 확인하고 보여줘라.
```

### SEC-STEP 3 — 입력검증 & 무료분석 보안 (STEP 3과 함께)
```
1. /api/analyze 입력(URL/파일)을 zod로 검증. URL 형식·도메인 검증.
2. 업로드 스크린샷은 MIME/용량 검증, 이미지만 허용.
3. 무료분석 1회 제한은 서버에서 free_analyses 존재 여부로 강제(클라 신뢰 금지).
4. /api/analyze에 rate limiting (어뷰징 방지).
```

### SEC-STEP 4 — 업로드 & 신청 보안 (STEP 4와 함께)
```
1. Supabase Storage는 비공개 버킷 + 만료 서명 URL.
2. 업로드 파일: MIME/확장자/용량 검증, 파일명 난수화, 실행파일 차단.
3. 사업자번호 등 개인정보는 저장 시 RLS 보호, 표시 시 마스킹.
4. 개인정보 수집 동의 기록을 저장.
5. 신청 API에 rate limiting + 입력 전수 zod 검증.
```

### SEC-STEP 5 — 결제 보안 (STEP 5와 함께)
```
1. 결제 금액은 서버에서 product_type 기준 재계산. 클라 금액 신뢰 금지.
2. 토스 웹훅은 서명 검증으로 위조 차단.
3. 결제 confirm 시 orderId·amount·중복결제 검증.
4. paymentKey 외 카드정보는 저장하지 않음(토스 위임).
5. 결제 관련 로그에 민감 토큰 마스킹.
```

### SEC-STEP 6 — 어드민 보안 (STEP 6과 함께)
```
1. admin은 RBAC + 서버단 재확인. (권장) 2FA 또는 IP 화이트리스트.
2. 모든 관리자 행동을 admin_logs에 감사 기록(행위자/대상/시각).
3. 어드민에서 개인정보 표시 시 마스킹, 최소노출.
4. 어드민 API도 rate limiting + CSRF 보호.
```

### SEC-STEP 7 — 워커 & 자격증명 보안 (STEP 7과 함께, 최우선)
```
가장 민감한 구간이다. 신중하게 구현하라.
1. blog_credentials 테이블: 자격증명은 AES-256-GCM 암호화 후 저장.
   평문 저장 절대 금지.
2. 암호화 마스터키는 워커 환경변수/시크릿 매니저에만. web과 공유 금지.
3. 복호화는 발행 실행 순간 메모리에서만, 작업 후 즉시 폐기.
4. web→worker는 X-Worker-Secret 검증 + (권장) HMAC 서명·타임스탬프.
5. 워커는 외부 직접 노출 금지(web IP만 허용). 외부통신 도메인 화이트리스트.
6. 워커 로그에 자격증명/토큰 미출력(마스킹).
구현 후 자격증명이 평문으로 남는 경로가 없는지 전 구간 점검 결과를 보여줘라.
```

---

## 10. 출시 전 보안 점검 체크리스트

### 시크릿/키
- [ ] 코드/깃에 하드코딩된 시크릿 없음 (스캔 완료)
- [ ] .env가 .gitignore에 포함, service_role 키 클라 미노출
- [ ] 자격증명 암호화 마스터키가 DB와 분리 보관

### 인증/인가
- [ ] 세션 HttpOnly/Secure/SameSite, 토큰 localStorage 미사용
- [ ] admin RBAC + 서버단 재확인, (권장) 2FA/IP 제한
- [ ] 로그인 브루트포스 제한, OAuth state/redirect 검증

### 데이터
- [ ] 전 테이블 RLS 활성화 + 기본 거부 정책
- [ ] 블로그 자격증명 AES-256-GCM 암호화 저장
- [ ] 개인정보 마스킹, URL 쿼리에 민감정보 미포함
- [ ] Storage 비공개 버킷 + 서명 URL

### 웹/API
- [ ] 보안 헤더(CSP/HSTS/X-Frame-Options 등) 적용
- [ ] 전 API zod 검증, 결제 금액 서버 재계산
- [ ] 주요 API rate limiting, CSRF 보호
- [ ] 에러 메시지 일반화(스택 미노출), 민감정보 로깅 마스킹

### 워커
- [ ] web→worker 시크릿/서명 인증, 워커 외부 미노출
- [ ] 자격증명 복호화는 발행 순간 메모리에서만
- [ ] 외부통신 도메인 화이트리스트

### 결제/웹훅
- [ ] 토스 웹훅 서명 검증, 중복결제·금액 검증
- [ ] 카드정보 직접 저장 없음(토스 위임)

### 운영
- [ ] 백업 암호화 및 접근 통제
- [ ] 키 회전 절차 문서화
- [ ] 침해 의심 시 대응 절차(키 회전/세션 무효화) 마련

---

## 11. 사고 대응 (Incident Response) 최소 절차

1. **탐지**: 비정상 로그인/대량 발행/무단 워커 호출 모니터링 알림.
2. **차단**: 의심 계정 세션 무효화, 해당 시크릿/키 즉시 회전.
3. **격리**: 워커 호출 일시 중단(수동 트리거라 차단 용이).
4. **조사**: admin_logs / 접근로그로 영향 범위 파악.
5. **통지**: 개인정보 유출 시 법정 통지 의무 확인(개인정보보호법).
6. **복구**: 자격증명 재발급 안내, 패치 후 재가동.

> 수동 트리거 구조라 사고 시 "발행을 즉시 멈출 수 있다"는 점이
> 보안상 강력한 장점이다. 이 통제권을 항상 유지한다.
